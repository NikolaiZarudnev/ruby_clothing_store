
<p>
  <strong>Title:</strong>
  <%= @category.title %>
</p>

<h2>Garments</h2>
<%= render @category.garments %>

<h2>Add a garment:</h2>
<%= render 'garments/form' %>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>

